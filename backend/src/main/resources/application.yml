spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/xj2j_project-board
    username: postgres
    password: f28sSca72yd
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect